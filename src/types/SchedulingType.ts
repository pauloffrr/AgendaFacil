export interface SchedulingProps {
    idScheduling: number;
    title: string;
    startDate: string;
    startHour: string;
    endDate: string;
    endHour: string;
    status: string;
    color?: string;
}